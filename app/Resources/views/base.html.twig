<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}<link href="{{asset('gigondas.css')}}" rel="stylesheet" type="text/css" />{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <img width="1200px" src="{{asset('BandeauValenceIUT.jpg')}}" />
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <a href="{{ path('logout') }}" >Maison out </a>
        {% else %}
        <a href="{{ path('login')}}" >Maison in </a>
        {% endif %}
        {% block body %}{% endblock %}
        {% block styleblock %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
